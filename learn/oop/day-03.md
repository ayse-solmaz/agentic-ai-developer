# OOP Day 3 — Constructors

**Learned:** Validate in `__init__`, defaults, `@staticmethod` factory.

- `__init__`: reject negative balance / empty name → `ValueError`
- Default: `transaction_history = []`
- Factory: `BankAccount.open_empty(account_number, owner_name)`

**Code:** [day-03-bank.py](./practice/day-03-bank.py)