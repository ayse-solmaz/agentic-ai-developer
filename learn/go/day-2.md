# Day 2 — Control Flow

**Status:** Done (2026-08-30)  
**Phase:** 1–5 Go Fundamentals — day 2

## Goal

Branch and repeat: `if`/`else`, `for` (counter, condition, `range`), `switch`, `continue`/`break`/labels.

## Dictionary

| Term | Meaning |
|------|---------|
| Control flow | Order statements run |
| Range | `for i, v := range slice` |
| `continue` | Skip this iteration |
| `break` | Stop the loop (`break Outer` stops the labeled outer loop) |

## Check (your run)

- Grades: B / A / C from score
- `n: 0` `1` `2`; countdown; `range` names
- `full access`, `mild`
- `loop: 0` `1` `3`; nested stop at `1 1`

## Next

Day 3 — functions and packages.
